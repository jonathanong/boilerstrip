[package]
name = "boilerstrip-node"
version = "0.1.4"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
boilerstrip = { path = "../crate" }
napi = { version = "3", features = ["napi4", "serde-json"] }
napi-derive = "3"
serde_json = "1.0"

[build-dependencies]
napi-build = "2"
